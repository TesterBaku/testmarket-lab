# Copy this file to ".env" to override defaults (all are optional).

# Port the server listens on
PORT=3000

# Secret used to sign sessions (any random string is fine for local use)
JWT_SECRET=testmarket-lab-secret

# Set to "development" to see detailed error messages
NODE_ENV=development
